%ol.post-list
  - posts.each do |post|
    %li.post-item
      %a{href: url(post.path)}
        .post-item-header
          %b #{ post.title }
          %time #{ post.created_at.strftime '%Y-%m-%d' }
        = post.description
